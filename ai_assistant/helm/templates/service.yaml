apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-assistant-safe.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "open-assistant-safe.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000